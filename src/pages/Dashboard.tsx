import { PageShell } from './PageShell';

export function Dashboard() {
  return (
    <PageShell
      eyebrow="App"
      title="Dashboard"
      description="Placeholder for a future signed-in dashboard with saved reports, recent drafts, and user-specific workflow shortcuts."
    >
      <p>The current full prototype dashboard remains preserved at the homepage route.</p>
    </PageShell>
  );
}
