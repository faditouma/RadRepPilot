import { PageShell } from './PageShell';

export function Signup() {
  return (
    <PageShell
      eyebrow="Account"
      title="Signup"
      description="Signup placeholder for a future account creation flow."
    >
      <p>User registration is intentionally deferred until the app has a backend/authentication plan.</p>
    </PageShell>
  );
}
