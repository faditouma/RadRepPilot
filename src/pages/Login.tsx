import { PageShell } from './PageShell';

export function Login() {
  return (
    <PageShell
      eyebrow="Account"
      title="Login"
      description="Authentication placeholder for a future user account workflow."
    >
      <p>Login is intentionally not connected in this phase. No Supabase or backend has been added.</p>
    </PageShell>
  );
}
