import { PageShell } from './PageShell';

export function About() {
  return (
    <PageShell
      eyebrow="About"
      title="About RadRepPilot"
      description="RadRepPilot is a prototype radiology workflow cockpit for structured reporting, concise requisitions, calculators, and follow-up language."
    >
      <p>This placeholder page preserves space for a fuller project story, validation notes, and portfolio context.</p>
    </PageShell>
  );
}
