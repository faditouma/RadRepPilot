import { PageShell } from './PageShell';

export function Preferences() {
  return (
    <PageShell
      eyebrow="Settings"
      title="Preferences"
      description="Placeholder for future user preferences, reporting style defaults, and local protocol settings."
    >
      <p>Preferences are not persisted yet. This route is prepared for a later authenticated app phase.</p>
    </PageShell>
  );
}
